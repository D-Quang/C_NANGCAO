#include<iostream>
#include<set>
#include<bits/stdc++.h>
using namespace std;

int main(){
    /*
    //Đếm số lượng phần tử khác nhau trong mảng số nguyên
    cout<<"Nhap so lan muon test: ";
    int t; cin>>t; //Số lần test 
    while(t--){
        cout<<"Nhap so phan tu moi lan test: ";
        int n; cin>> n; //So phan tu
        set<int> se;
        for (int i=0; i< n; i++){
        int x; cin >> x;
        se.insert(x);
        }
        cout << "So phan tu khac nhau trong mang la: "<<se.size() << endl;
    }
    */

    //Cho 1 mảng phần tử số nguyên hãy kiểm tra xem phần tử đó có trong mảng hay không
    /*cout<<"Nhap so luot truy van: ";
    int t; cin >> t;
    while(t--){
        cout<<"Nhap so phan tu:";
        int n; cin >> n;
        set<int> se;
        for (int i= 0; i <n; i++){
            int x; cin >> x;
            se. insert(x);
        }
        cout<<"Nhap so phan tu muon truy van";
        int q; cin >> q;
        while(q--){
            cout<<"Nhap phan tu muon truy van";
            int x; cin >> x;
            if(se.count (x) == 1){
                cout << "YES\n";
            }else{ 
                cout << "NO\n";
            }
        }
    }
    */

   //Số xuâts hiện nhiều nhất trong mảng
   
}